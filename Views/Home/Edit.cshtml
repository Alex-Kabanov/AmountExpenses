@using AmountExpenses.Models;
@model AmountExpenses.Models.Expense

@{ 
    
}
<div>
    <form class="form" asp-controller="Home" asp-action="Edit" method="post">
        <input asp-for="Id" type="hidden" />
        <div class="form">
            <label>Title</label>
            <input asp-for="Title" />
        </div>
        <div>
            <label>Amount</label>
            <input asp-for="Amount" />
        </div>
        <div>
            <label>Categories</label>
            <select asp-for="Categories" asp-items="Html.GetEnumSelectList<Categories>()"></select>
        </div>
        <input type="submit"  value="Submit"/>

    </form>
</div>
